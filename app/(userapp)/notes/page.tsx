import MakeNote from "@/components/notes/MakeNote";

const Notes = () => {
  return (
    <section className="ml-16 grid h-screen grid-cols-2 p-8">
      <MakeNote />
    </section>
  );
};

export default Notes;
